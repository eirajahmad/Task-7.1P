<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Application tag starts here -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">

        <!-- MainActivity: This is the entry point of the app -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <!-- This category makes the activity serve as the app's launch activity -->
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ItemListActivity: This activity is for viewing the list of items -->
        <activity android:name=".ItemListActivity" />

        <!-- AddItemActivity: This activity is used to add new items -->
        <activity android:name=".AddItemActivity" />
        <activity android:name=".ItemDetailActivity" />
    </application>

</manifest>
